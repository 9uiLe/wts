# wts の作業規約

この規約はリポジトリ全体に適用する。利用手順は [README.md](README.md)、開発・検証手順は [docs/development.md](docs/development.md)、責務と設計方針は [docs/design.md](docs/design.md) を参照する。

## 作業の契約

日本語で簡潔かつ丁寧に回答する。作業を始める前に、要求された成果と達成を証明する最小限の受け入れ基準を `contract` として明示する。依頼が曖昧なら確認し、確認できない場合は明示された意図に沿う最小限の解釈と仮定を記録する。

計画、変更、テスト、レビュー指摘、追加調査はすべて作業候補 `claim` として扱う。その作業を除くと、実際の入力と環境において `contract` が未達成または未証明になる場合だけ採用する。有用性や指摘者の重要度評価だけでは作業を追加しない。

各 claim を最小限の信頼できる行為で実行し、規模に見合う証拠で証明する。実行だけでは完了とせず、証明済みの項目を理由なく再検証しない。contract が証明され、採用すべき claim がなくなった時点で終了する。報告は成果、証拠、注意に値する不採用の claim に限る。不採用項目を修正やフォローアップへ振り替えない。

本リポジトリの停止規則として、修正・検証を 3 ラウンド終えたら必ず停止し、未解決項目を報告する。第 n+1 ラウンドで提起された claim が第 n ラウンドですでに確認可能だった場合、その claim は採用しない。

上限、閾値、タイムアウト、再試行回数などを独自に設けない。値を定められる根拠は、依頼者の明示的指定、適用される技術契約、プロジェクトポリシー、contract の達成・証明に必要な測定証拠に限り、根拠を明示する。測定値を根拠なく合否基準や推奨値へ転用しない。必要な値の判断が未確定なら決定権者へ確認する。

作業は原則として適切な粒度で subagent に委譲し、担当範囲、手順、完了基準を伝える。全体の計画と進行を管理し、委譲しない方が妥当な作業は自ら実行してよい。

## 情報の責務

- How はコードの命名、構造、データの流れで表し、コメントで処理を言い換えない。
- What は観測可能な振る舞いを検証するテストで表す。contract が求めない内部構造を固定しない。
- Why はコミットメッセージまたは作業ログに記録する。
- Why not は外部制約や非自明なトレードオフとして、関係するコード付近のコメントに記録する。

同じ説明をコード、テスト、変更履歴、コメントへ重複させない。README は外部利用者向けに利用方法を記述する。開発・環境構築の資料は docs/ に置き、README から参照する。文書は会話や作業経緯を知らない読者が理解できる、自立した文章で記述する。

## 実装と依存管理

- CLI 名は `wts` とする。TypeScript、Bun、Commander、`@clack/prompts` を使用し、Chalk・Ora は具体的な必要性がある場合だけ追加する。
- 対象は Apple Silicon macOS。Nix のシステム名は `aarch64-darwin`、Bun のビルドターゲットは `bun-darwin-arm64` とする。
- Nix Flakes が開発ツール、Bun が JavaScript / TypeScript 依存を管理する。devShell 内の固定された Bun を使用する。
- `flake.lock` と `bun.lock` を Git 管理する。通常のインストールは `bun install --frozen-lockfile --ignore-scripts` とし、未レビューの依存更新、ロックファイル差し替え、インストールスクリプト実行を行わない。
- 依存追加・更新時は公開元、ライセンス、リリース履歴、既知の脆弱性、スクリプト、推移的依存、予期しない通信を確認し、更新理由を記録する。`bun audit` と `osv-scanner` の監査結果を確認する。
- TypeScript・JSON の整形と lint には `biome.json` の Biome 設定を使用する。整形は既定設定、lint は recommended ルールとし、型の検証には `tsc --noEmit` を使用する。Bun による実行や lint を型検査の代わりにしない。
- 外部入力をシェル文字列へ連結しない。引数配列または安全な API を使う。対話のキャンセルは正常終了として扱う。
- CLI は実行時に外部コマンド・ファイルを要求しない。外部要件を追加する場合は README とビルド情報を更新し、欠落時の振る舞いを検証する。

## 成果物と公開

- `dist/` と `release/` は Git 管理しない。バイナリ、SHA-256、`BUILD_INFO` を一組として検証し、秘密情報をログや成果物へ含めない。
- CI アクションをコミット SHA に固定し、最小権限を設定する。通常検証とリリース公開を分離する。
- ビルドスクリプトの出力は検証用とする。正式公開には [開発資料のリリース条件](docs/development.md#正式リリースの条件) を満たす必要がある。最低対応 macOS、署名・公証の採否、対象環境での検証が未確定のまま正式リリースを行わない。
- 正式リリースはレビュー済みコミットとクリーンな作業ツリーから生成し、Apple Silicon 上で実行検証する。
- 署名・公証を採用する場合は最終バイナリへ適用・検証した後にチェックサムを生成する。採用しない場合は Gatekeeper の制約と検証済み手順をリリース本文に記載する。

## 検証の選択

[開発資料のコマンド](docs/development.md#開発コマンド)から、変更の達成を証明するために必要な検証を選ぶ。整形検査、lint、型チェック、CLI テスト、依存監査、ビルドとチェックサム照合はそれぞれ別の証拠として扱う。整形の修正には `bun run format`、変更しない検査には `bun run format:check` と `bun run lint` を使用する。CI は `bun run check` で整形検査、lint、型チェック、テスト、ビルドを実行する。

対話を変更した場合は、TTY 上で `bun run dev doctor --interactive` の肯定入力、否定入力、Ctrl-C によるキャンセルを確認する。実行結果と未検証事項を区別して報告する。
