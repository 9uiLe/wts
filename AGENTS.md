# エージェント向け作業規約

## 進め方

- 日本語で簡潔かつ丁寧に回答する。
- 作業前に contract（要求された成果と、その達成を証明する最小限の受け入れ基準）を明示する。曖昧な依頼は確認し、確認できない場合は意図に沿う最小限の解釈と仮定を記録する。
- 計画、変更、テスト、レビュー指摘、追加調査をすべて claim として扱う。その作業を除くと contract が未達成または未証明になる場合に限って実行する。
- 各 claim は最小限の信頼できる行為で解決し、規模に見合う証拠で証明する。証明済みの項目を理由なく再検証しない。
- contract の達成を証明したら終了し、成果・証拠・注意に値する不採用の claim だけを報告する。
- ユーザー指定のヒューズとして、修正・検証は 3 ラウンドを終えたら必ず停止し、未解決項目を報告する。前ラウンドですでに確認できた新規 claim は次ラウンドで採用しない。
- 上限、閾値、タイムアウト、再試行回数などは独自に設けない。ユーザー指定、適用される技術契約、プロジェクトポリシー、必要性を裏付ける測定証拠から導出し、根拠を明示する。
- 作業は原則として適切な粒度で subagent に委譲し、担当範囲・手順・完了基準を伝える。全体の計画と進行を管理し、委譲しない方が妥当な作業は自ら実行してよい。

## 情報の置き場所

- How はコードの命名・構造・データの流れで表す。コメントで処理を言い換えない。
- What は観測可能な振る舞いを検証するテストで表す。contract が求めない内部構造を固定しない。
- Why はコミットメッセージまたは作業ログに記録する。
- Why not は外部制約や非自明なトレードオフとして、関係するコード付近のコメントに記録する。
- 同じ説明をコード、テスト、変更履歴、コメントへ重複させない。

## 技術上の契約

- CLI 名は `wts`。TypeScript、Bun、Commander、`@clack/prompts` を使用する。Chalk・Ora は具体的な必要性が生じた場合だけ追加する。
- 開発対象は Apple Silicon macOS。Nix のシステム名は `aarch64-darwin`、Bun のビルドターゲットは `bun-darwin-arm64` とする。
- Nix Flakes が開発ツールを、Bun が JavaScript / TypeScript 依存を管理する。devShell 内の固定された Bun を使用する。
- `flake.lock` と `bun.lock` を Git 管理する。通常のインストールでは `bun install --frozen-lockfile --ignore-scripts` を使用し、未レビューの依存更新・ロックファイル差し替え・インストールスクリプト実行を行わない。
- 依存追加・更新時は公開元、ライセンス、リリース履歴、既知の脆弱性、スクリプト、推移的依存、予期しない通信を確認し、更新理由を記録する。`bun audit` と `osv-scanner` の監査結果を確認する。
- Bun の TypeScript 実行だけで型検査済みと扱わず、`tsc --noEmit` を実行する。
- 外部入力をシェル文字列へ連結しない。引数配列または安全な API を使う。対話のキャンセルは正常終了として一貫して扱う。
- 現在の CLI に実行時の外部コマンド・ファイル要件はない。機能追加で必要になる場合は README とビルド情報の外部要件を更新し、欠落時の振る舞いを検証する。
- `dist/`・`release/` は Git 管理しない。バイナリ、SHA-256、`BUILD_INFO` を一組として検証する。秘密情報をログや成果物へ含めない。
- CI アクションはコミット SHA に固定し、最小権限を設定する。通常検証とリリース公開を分離する。
- 最低対応 macOS、署名・公証の採否、対象環境での検証が未確定な間は検証用ビルドとして扱う。正式リリースはレビュー済みコミットとクリーンな作業ツリーから生成し、Apple Silicon 上で実行検証する。
- 署名・公証を行う場合は最終バイナリへ適用・検証した後にチェックサムを生成する。行わない場合は Gatekeeper の制約と検証済み手順をリリース本文に記載する。

## 検証コマンド

リポジトリのルートで `nix develop` を実行する。

```bash
nix flake check
bun install --frozen-lockfile --ignore-scripts
bun run verify:deps
bun run typecheck
bun run test
bun run build
./dist/wts-macos-arm64 --help
./dist/wts-macos-arm64 --version
```

チェックサムは `release/` で `shasum -a 256 -c wts-macos-arm64.sha256` を実行する。対話を変更した場合は `bun run dev doctor --interactive` で入力・キャンセルを確認する。変更に応じた必要な検証を選び、実行結果と未検証事項を区別して報告する。
