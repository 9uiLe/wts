# wts の作業規約

この規約はリポジトリ全体に適用する。日本語で簡潔かつ丁寧に回答し、参照資料は作業に必要な節だけを読む。

| 作業 | 参照先 |
| --- | --- |
| CLI の利用・外部要件・操作の復旧 | [README.md](README.md) |
| 設定・命名・管理外ファイルのコピー | [docs/configuration.md](docs/configuration.md) |
| ドメイン・永続状態・振る舞い・安全契約の変更 | [docs/design.md](docs/design.md)と該当するテスト |
| 環境構築・開発セッション | [開発環境](docs/development.md#開発環境)、[開発セッション](docs/development.md#開発セッション) |
| 実行コマンド・検証の選択・結果の記録 | [開発コマンド](docs/development.md#開発コマンド)、[検証を実行する](docs/development.md#検証を実行する) |
| AI 向けスキルの編集 | [AI 向けスキルの保守](docs/development.md#ai-向けスキルの保守)。CLI のソース・文書編集だけでは操作用スキルを起動しない |

## 作業の契約と完了条件

着手前に、要求された成果と最小限の受け入れ基準を `contract` として明示する。成果や権限に影響する曖昧さは確認し、回答に依存しない作業は進める。依頼範囲内の実装上の選択は意図に沿う最小限の解釈で進め、仮定を記録する。

計画・変更・検証・レビュー指摘・追加調査を作業候補 `claim` として扱う。実際の入力と環境で、その作業を除くと contract が未達成または未証明になる場合だけ採用する。有用性や指摘者の重要度評価だけで作業を増やさず、不採用項目を修正やフォローアップへ振り替えない。

採用した claim は最小限の信頼できる行為で実行し、規模に見合う証拠で達成を示す。証明済みの項目を理由なく再検証しない。contract が証明され、採用すべき claim がなくなったら終了し、成果・証拠・注意に値する不採用項目を報告する。

修正・検証を 3 ラウンド終えたら必ず停止し、未解決項目を報告する。第 n+1 ラウンドで提起された claim が第 n ラウンドですでに確認可能だった場合、その claim は採用しない。

上限・閾値・タイムアウト・再試行回数などの値は、依頼者の明示的指定、適用される技術契約、プロジェクトポリシー、contract の達成・証明に必要な測定証拠を根拠とし、その根拠を明示する。測定値を根拠なく合否基準や推奨値へ転用しない。必要な値が未確定なら決定権者へ確認する。

独立した調査・実装・検証は subagent が利用可能なら委譲し、担当範囲・手順・完了基準を伝える。小さな修正や分担によって確認・統合作業が増える作業は自ら実行する。委譲先の指摘にも同じ claim の採用条件を適用する。

## 自律的に進める範囲と承認

依頼範囲内の調査・編集・ローカル検証・変更に起因する失敗の修正は、段階ごとの承認を待たず、必要な証拠が揃うまで進める。自動テストは一時 Git リポジトリ・bare origin とテスト用応答を使う。

実際のリモートへの push、worktree・ブランチ・残存ファイルの削除、公開、共有設定やインストール済みスキルの置き換えは、依頼・既存の承認がその対象と影響を含む場合に限る。確認するのは未承認の範囲だけとし、その承認に依存しない準備・検証は完了させる。

コマンド例や `--yes`・`--push`・`--force` は権限を与えない。削除対象・残存ファイルの照合、UI 基準版の明示的採用、正式リリースの承認と条件に従い、未確定の承認を仮定で補わない。

## 情報の責務と変更単位

処理の方法は命名・構造・データの流れで表し、コメントで言い換えない。観測可能な振る舞いはテストで表し、contract が求めない内部構造を固定しない。判断理由はコミットメッセージまたは作業ログへ、外部制約や非自明なトレードオフは関係するコード付近へ記録し、同じ説明を重複させない。

README は利用者向けの操作、docs/ は設定・設計・開発の正本とする。文書は会話や作業経緯を知らない読者が理解できる文章にする。検証結果は対象コミットを特定できる PR・CI・Release に記録する。

目的が共通する実装・テスト・文書は一つの変更単位としてコミットし、独立した目的は分ける。件名に対象と変更内容、必要な本文に目的・判断理由を記し、各コミットの差分とメッセージで作業を理解できる状態にする。

PR は [PR テンプレート](.github/PULL_REQUEST_TEMPLATE.md)の必須項目に従う。base との差分に含まれる全コミットの説明、履歴変更後のハッシュ・説明の更新、受け入れ条件に対する結果と未検証事項を省略しない。

## 実装・依存・配布の条件

- CLI 名は `wts`。TypeScript、Bun、Commander を使用し、利用者との入出力は `9uiLe/hamio` に集約する。追加依存には具体的な必要性を求める。
- 対象は Apple Silicon macOS。Nix は `aarch64-darwin`、Bun のビルドターゲットは `bun-darwin-arm64` とする。
- Nix Flakes が開発ツール、Bun が JavaScript / TypeScript 依存を管理する。devShell 内の固定 Bun を使い、`flake.lock` と `bun.lock` を Git 管理する。
- 通常の依存取得は `bun install --frozen-lockfile --ignore-scripts`。未レビューの依存更新、ロックファイル差し替え、インストールスクリプト実行を行わない。追加・更新には [依存の検証](docs/development.md#依存の検証)の調査・両監査と理由の記録を必須とする。
- TypeScript・JSON は `biome.json` の既定の整形と recommended lint を使う。型は `tsc --noEmit` で検証し、Bun の実行や lint で代替しない。
- 外部入力はシェル文字列へ連結せず、引数配列か安全な API で渡す。対話のキャンセルは正常終了として扱う。
- 配布バイナリは実行時の Nix・Bun・Node.js・ソースファイルを要求しない。全コマンドで PATH 上の `hamio` を使用し、操作ごとの Git・gh・設定・コピーリスト・命名スクリプトの要件は README と設定資料に従う。外部要件の追加時は README・ビルド情報を更新し、欠落時の振る舞いを検証する。
- `dist/` と `release/` は Git 管理せず、バイナリ・SHA-256・`BUILD_INFO` を一組として検証する。秘密情報をログ・成果物へ含めない。
- CI アクションはコミット SHA に固定し、最小権限にする。通常検証とリリース公開を分離する。
- ビルド出力は検証用とする。正式公開には [正式リリースの条件](docs/development.md#正式リリースの条件)をすべて満たす必要がある。最低 macOS、署名・公証の採否、対象環境の検証が未確定のまま正式公開しない。

## 検証

[変更の影響に応じた必須検証](docs/development.md#検証を実行する)を選び、Bun の個別コマンドは `nix develop --no-update-lock-file` の devShell 内で実行する。整形検査・lint・型チェック・テスト・依存監査・ビルドと成果物照合を別の証拠として扱い、実施結果と未検証事項を区別する。

整形の修正は `bun run format`、変更しない検査は `bun run format:check` と `bun run lint`。CI は `bun run check` で整形検査・lint・型チェック・テスト・ビルドを実行する。対話を変更した場合は、TTY 上で `bun run dev doctor --interactive` の肯定・否定・Ctrl-C によるキャンセルを必ず確認する。
